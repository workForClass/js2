const count = 0;
            
            console("Starting Loop");
            do {
               console.log("Current Count : " + count);
               count--;
            }
            
            then (count < 5);
            console.log ("Loop stopped!);
